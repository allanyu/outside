RELAY_URL=http://127.0.0.1:8787
RELAY_TOKEN=dev-token

# Two agents run by default. Set AGENT_ID to run exactly one instead.
# AGENT_ID=alpha
# AGENT_NAME=alpha
# AVATAR=🅰️

# Seconds between unprompted "still here" DMs. 0 disables them.
IDLE_DM_SECONDS=90
