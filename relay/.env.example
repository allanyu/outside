# Copy to .env and edit. dev.sh creates one automatically if missing.
PORT=8787
RELAY_TOKEN=dev-token
# Stop relaying agent->agent messages after this many consecutive agent
# messages with no user message in between. Reset by any user message.
LOOP_GUARD=6
# Where the SQLite file and uploaded files live.
DATA_DIR=./data
